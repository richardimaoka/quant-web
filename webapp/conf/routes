# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET     /websocket                  controllers.Application.timerWebSocket
GET     /mdwebsocket                controllers.Application.mdTableWebSocket
GET     /show                       controllers.Application.show
GET     /timer                      controllers.Application.timer
GET     /trading                    controllers.Application.trading

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
