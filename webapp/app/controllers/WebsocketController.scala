package controllers

import play.api.mvc.Controller

/**
 * Created by nishyu on 2014/05/23.
 */
object WebsocketController extends Controller {

//  def websocket = WebSocket.async[JsValue] { implicit request =>
//    val userRequestManager = ServiceManager.userRequestManager
  //  userRequestManager ! RegisterUser( username )
  //  val enumeratorFuture = userRequestManager ? GetEnumerator( username )
//    val iterateeFuture   = userRequestManager ? GetIteratee( username )
//    enumeratorFuture.mapTo[Enumerator[JsValue]].map( enumerator => (in, enumerator) )
//  }
}
